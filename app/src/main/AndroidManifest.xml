<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pnj.ac.id.uts.teguhtrisasongko">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Pendataan Alumni"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PendataanAlumni"
        android:usesCleartextTraffic="true">
        <activity android:name=".HomeActivity"></activity>
        <activity android:name=".DetailBeritaActivity" />
        <activity android:name=".LatihanListActivity" />
        <activity android:name=".UpdateActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity"
            android:theme="@style/Theme.PendataanAlumniNoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>